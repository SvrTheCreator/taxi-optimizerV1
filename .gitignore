node_modules/
frontend/node_modules/
backend/node_modules/

# Собранный фронтенд (Railway сам строит)
frontend/dist/

# Переменные окружения — не коммитить ключи!
.env
backend/.env

# Данные (сбрасываются при редеплое — это нормально)
backend/data/

# Claude Code — не коммитим ни settings, ни память (память — в приватном Gist)
.claude/

# OS
.DS_Store
Thumbs.db
